You are a senior AI systems architect and backend engineer in building production-grade AI agent systems using FastAPI, local LLMs, hybrid databases, Discord bots, and email automation.
Project overview: Build a focused ERP system handling only Bill of Materials (BOM) and Inventory Management. The system uses natural language input in English and an autonomous agentic LLM with MCP tools.
Core requirements:
LLM and MCP tooling: Use local gemma4:26b. The LLM accepts natural language input, decides which MCP tool to call, and dynamically generates SQL h minimal hardcoding. Required tools are check_inventory, update_inventory, create_bom, get_bom, update_bom, delete_bom. Use true agentic flow like ReAct not static pipelines.
Database design: Use MySQL local for structured data like inventory and BOM tables. Provide complete schemas for both use respective when needed or use both. if i define an item in bom that it requires <item 1>, <item 2>....<item n> and run it,<items> are in inventory database, it is fetched and respective inventories required are blocked saying in use( this is what the backend output should be)
now an additional feature is, if say i need <bom1> <number=10> it should check <inventory for= <bom1>>itself if not present it can look for <items1...n> in <bom> and backend output results in <items1.....n> are present and this <bom1> can be built in 2 weeks
Backend: I have made a backend that generates sql queries and performs the respective actions to db based on query, check it once if everything goes right, i also have a discord bot, this bot takes input on discord and passes it to llm that creates query and backend works then the output is given by discord bot
Frontend integration: I have existing simple frontend with input field. Provide full integration instructions to connect it to the new FastAPI backend. Detail the flow, API design, request response formats, and response handling.
system basically has 2 frontends, one will be the discord server where my bot exists and other will be where i have written a code in frontend directory
autonomous email system: i have already setup node mailer in erp mail system directory, it must be linked to real time responses, for eg if <bom1> needs <items 1...n> but <item1> <item2> are short by <required quantity>= realtime value - current quantity, the shortage is displayed and a message saying do you want to order this is displayed, when user types yes, an email is sent to the vendor with <required quantity> saying kindly deliver <required quantity> items regards saurav
Constraints and goals:Prioritize working prototype first. Avoid over-engineering but plan for scaling. Some minimal hardcoding ok for speed. Final system is smart ERP assistant using natural language that works via web frontend, Discord, and email.